'use client';import {useMemo,useState} from 'react';import {products} from '@/lib/products';import {ProductGrid} from '@/components/store';export default function SearchPage(){const [q,setQ]=useState('');const items=useMemo(()=>products.filter(p=>(p.name+' '+p.category).toLowerCase().includes(q.toLowerCase())),[q]);return <div className="container py-16"><p className="text-xs uppercase tracking-[.2em] text-black/45">Search</p><div className="border-b border-black/30 mt-5"><input autoFocus value={q} onChange={e=>setQ(e.target.value)} placeholder="What are you looking for?" className="bg-transparent outline-none w-full text-4xl md:text-6xl py-5 tracking-[-.05em]"/></div>{q&&<div className="mt-12">{items.length?<ProductGrid items={items}/>:<div className="py-24 text-center"><h2 className="text-4xl">No results.</h2><p className="text-black/50 mt-3">Try another search.</p></div>}</div>}</div>}