import {products} from '@/lib/products';import {ProductGrid} from '@/components/store';export default async function Collection({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const title=slug.replaceAll('-',' ');const items=products.filter(p=>p.collection.toLowerCase().replaceAll(' ','-')===slug);return <div className="container py-14"><p className="text-xs uppercase tracking-[.2em] text-black/45">RTB / Collection</p><h1 className="text-7xl md:text-9xl tracking-[-.08em] capitalize mt-3">{title}</h1><p className="max-w-xl text-black/55 mt-5 leading-7">A focused RTB edit for the season.</p><div className="mt-14"><ProductGrid items={items.length?items:products.slice(0,8)}/></div></div>}